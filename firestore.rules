rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * ValutaBot Security Rules
     */

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    // Правила для кэша курсов (публичное чтение и запись для быстрой работы)
    match /rates_cache/{id} {
      allow read, write: if true;
    }

    // Правила для истории курсов (публичное чтение всем, запись только авторизованным)
    match /rates_history/{id} {
      allow get, list: if true;
      allow create: if isSignedIn();
      allow update, delete: if false; 
    }

    match /users/{userId} {
      allow get, list: if isOwner(userId);
      allow create: if isOwner(userId) && request.resource.data.id == userId;
      allow update: if isOwner(userId) && request.resource.data.id == resource.data.id;
      allow delete: if isOwner(userId);

      match /notifications/{notificationId} {
        allow get, list: if isOwner(userId);
        allow create: if isOwner(userId) && request.resource.data.userId == userId;
        allow update: if isOwner(userId) && request.resource.data.userId == resource.data.userId;
        allow delete: if isOwner(userId);
      }
    }

    match /currencies/{currencyId} {
      allow get, list: if true;
      allow create, update, delete: if false; 
    }

    match /exchange_rates/{exchangeRateId} {
      allow get, list: if true;
      allow create, update, delete: if false;
    }
  }
}