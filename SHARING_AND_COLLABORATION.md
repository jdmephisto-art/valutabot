
# 🚀 Инструкция по работе с Git и Push

Если вы столкнулись с ошибками при отправке кода, следуйте этой инструкции.

## Шаг 1: Подготовка (PowerShell)
Если терминал работает нестабильно (Segmentation fault), закройте его и откройте заново.

## Шаг 2: Настройка личности (однократно)
```powershell
git config --global user.email "your-email@example.com"
git config --global user.name "your-username"
```

## Шаг 3: Гарантированная отправка (Reset)
Если GitHub блокирует пуш из-за "secrets" или "large files", самый надежный способ — очистить локальную историю и отправить файлы как "новые".

> **Внимание:** Это удалит историю предыдущих коммитов локально, но решит проблему блокировки.

1. **Удаляем старую историю Git:**
   ```powershell
   Remove-Item -Path .git -Recurse -Force
   ```

2. **Инициализируем репозиторий заново:**
   ```powershell
   git init
   git checkout -b main
   ```

3. **Добавляем файлы (с учетом нового .gitignore):**
   ```powershell
   git add .
   git commit -m "Clean project state: fixed icons, SEO and security"
   ```

4. **Привязываем GitHub (используйте ваш токен):**
   Замените `[YOUR_TOKEN]` на ваш токен и `[USER/REPO]` на путь к вашему репозиторию.
   ```powershell
   git remote add origin https://[YOUR_TOKEN]@github.com/[USER/REPO].git
   ```

5. **Отправляем код:**
   ```powershell
   git push -u origin main -f
   ```

---
**Почему это важно:**
Файл `.gitignore` теперь автоматически исключает папку `node_modules` и файлы `*.zip`. Это предотвращает ошибки перегрузки памяти (Segmentation fault) и блокировки GitHub из-за превышения лимитов размера файлов.
