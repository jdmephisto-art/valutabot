
# 🚀 Инструкция по работе с Git и Пушем (Безопасная версия)

Эта инструкция поможет вам держать репозиторий в чистоте и избегать блокировок GitHub.

## 📁 Структура проекта в Git

**Что должно быть в репозитории:**
- `src/` — Исходный код (компоненты, хуки, API, логика).
- `public/` — Публичные активы (иконки, manifest.json, llms.txt).
- `docs/` — Документация и схемы данных.
- Корневые файлы: `package.json`, `next.config.ts`, `tsconfig.json`, `tailwind.config.ts`, `firestore.rules`.

**Что Git игнорирует (через .gitignore):**
- `node_modules/` — Библиотеки (устанавливаются через `npm install`).
- `.next/` — Временные файлы сборки.
- `*.zip` — Архивы и тяжелые файлы.
- `.env*` — Файлы с секретными ключами.

---

## 🛠 Шаг 1: Подготовка (PowerShell)
Если гит работает нестабильно или выдает ошибки соединения:
```powershell
# Сброс настроек прокси (если были)
git config --global --unset http.proxy
git config --global --unset https.proxy
```

## 🛠 Шаг 2: Настройка личности (однократно)
```powershell
git config --global user.email "your-email@example.com"
git config --global user.name "your-username"
```

## 🛠 Шаг 3: Гарантированная отправка (Чистая история)
Этот метод удаляет локальную историю (включая старые секреты и тяжелые файлы) и отправляет код как новую чистую версию.

1. **Удаляем старую историю Git:**
   ```powershell
   Remove-Item -Path .git -Recurse -Force
   ```

2. **Инициализируем репозиторий заново:**
   ```powershell
   git init
   git checkout -b main
   ```

3. **Добавляем файлы:**
   ```powershell
   git add .
   git commit -m "Final secure version: clean icons, SEO and optimized structure"
   ```

4. **Привязываем GitHub:**
   ```powershell
   git remote add origin https://github.com/jdmephisto-art/valutabot.git
   ```

5. **Отправляем код:**
   *(Откроется окно браузера для подтверждения входа)*
   ```powershell
   git push -u origin main -f
   ```

---
**Почему это важно:**
- **Чистота**: Благодаря `.gitignore` ваш пуш будет весить менее 1 МБ.
- **Безопасность**: Мы удалили все упоминания токенов из истории.
- **SEO**: Новые метаданные и структура заголовков теперь в основной ветке.
