
# 🚀 Инструкция по работе с Git и Push

Если вы столкнулись с ошибками при отправке кода или блокировкой со стороны GitHub, следуйте этой инструкции.

## Шаг 1: Подготовка
Если терминал работает нестабильно, закройте его и откройте заново.

## Шаг 2: Настройка личности (однократно)
```powershell
git config --global user.email "your-email@example.com"
git config --global user.name "your-username"
```

## Шаг 3: Гарантированная отправка (Чистая история)
Этот метод удаляет локальную историю (включая ошибочно добавленные тяжелые файлы и старые секреты) и отправляет код как новую чистую версию.

1. **Удаляем старую историю Git:**
   ```powershell
   Remove-Item -Path .git -Recurse -Force
   ```

2. **Инициализируем репозиторий заново:**
   ```powershell
   git init
   git checkout -b main
   ```

3. **Добавляем файлы:**
   ```powershell
   git add .
   git commit -m "Final secure version: clean icons, SEO and security"
   ```

4. **Привязываем GitHub:**
   Используйте стандартный URL вашего репозитория. При пуше Windows предложит войти через браузер или запросит токен в отдельном окне.
   ```powershell
   git remote add origin https://github.com/jdmephisto-art/valutabot.git
   ```

5. **Отправляем код:**
   ```powershell
   git push -u origin main -f
   ```

---
**Почему это важно:**
- **Безопасность**: Мы полностью очистили историю от любых старых токенов, которые мог видеть GitGuardian.
- **Производительность**: Файл `.gitignore` автоматически исключает тяжелые папки (`node_modules`), что делает работу Git мгновенной.
- **Брендинг**: Все иконки Firebase заменены на нейтральные финансовые логотипы в метаданных приложения.
