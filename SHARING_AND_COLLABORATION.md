
# 🚀 Как отправить код в GitHub (Решение проблем)

Если у вас возникли ошибки `403 Forbidden`, `Permission denied` или `fatal: unable to auto-detect email`, следуйте этой инструкции.

## Шаг 0: Исправление системной ошибки (Fork/Resource unavailable)
Если терминал пишет `bash: fork: retry: Resource temporarily unavailable`, это значит, что процессы зависли.
**Просто закройте текущую вкладку терминала и откройте новую.**

## Шаг 1: Настройка вашей личности в Git
Git должен знать, кто автор изменений. Выполните эти команды в терминале (подставьте свои данные):

```bash
git config --global user.email "your-email@example.com"
git config --global user.name "Your Name"
```

## Шаг 2: Создание токена доступа (PAT)
GitHub больше не принимает обычные пароли. Вам нужен **Personal Access Token**:
1. Перейдите в [GitHub Settings -> Tokens (Classic)](https://github.com/settings/tokens).
2. Нажмите **Generate new token (classic)**.
3. Выберите срок (например, 30 дней) и отметьте галочку **repo**.
4. Нажмите Generate внизу и **обязательно скопируйте токен** (он покажется один раз).

## Шаг 3: Гарантированная отправка кода (Push)
Чтобы обойти ошибку 403, мы добавим токен прямо в ссылку. Выполните команды по очереди:

```bash
# 1. Если проект еще не инициализирован
git init

# 2. Удаляем старую привязку, если она была
git remote remove origin 2>/dev/null

# 3. Привязываем заново С ВАШИМ ТОКЕНОМ (вставьте ваш токен вместо <TOKEN>)
# Пример: git remote add origin https://ghp_AbCd123...@github.com/jdmephisto-art/valutabot.git
git remote add origin https://<ВАШ_ТОКЕН_ИЗ_ШАГА_2>@github.com/jdmephisto-art/valutabot.git

# 4. Сохраняем изменения и отправляем
git add .
git commit -m "Secure version without hardcoded tokens"
git branch -M main
git push -u origin main
```

---
**Совет:** Теперь ваш код в безопасности. Токен Telegram удален из файлов и используется через переменные окружения Vercel.
