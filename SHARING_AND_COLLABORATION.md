
# 🚀 Исправление ошибок Git и Push (PowerShell / Bash)

Если вы видите ошибки `Segmentation fault`, `fork: Resource temporarily unavailable` или `remote helper 'https' aborted session`, это означает, что Git перегружен лишними файлами (node_modules).

## Шаг 1: Полная очистка терминала
1. **Закройте все окна терминала.**
2. **Откройте ОДНО новое окно PowerShell.** Это важно для очистки памяти.

## Шаг 2: Настройка личности (однократно)
Если Git ругается на "identity", выполните:
```powershell
git config --global user.email "jdmephisto@gmail.com"
git config --global user.name "jdmephisto-art"
```

## Шаг 3: Глубокая очистка индекса (PowerShell)
Эти команды заставят Git "забыть" тяжелые папки и использовать новый `.gitignore`:

```powershell
# 1. Удаляем индекс (сами файлы останутся на месте)
git rm -r --cached .

# 2. Добавляем файлы заново (теперь node_modules будет проигнорирован)
git add .

# 3. Создаем чистый коммит
git commit -m "Fix: ignore node_modules and cleanup index"
```

## Шаг 4: Привязка и Пуш с Токеном
Замените `<TOKEN>` на ваш GitHub Personal Access Token (PAT):

```powershell
# 1. Удаляем старый origin
git remote remove origin

# 2. Привязываем заново с вшитым токеном для обхода ошибки 403
# Формат: https://<TOKEN>@github.com/jdmephisto-art/valutabot.git
git remote add origin https://<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>@github.com/jdmephisto-art/valutabot.git

# 3. Отправляем код (Force push)
git push -u origin main -f
```

---
**Почему это сработает:** 
Созданный файл `.gitignore` исключает папку `node_modules` (в ней более 30,000 файлов). Без этого Git пытается "проглотить" их все, что вызывает падение процесса (Segmentation fault) в ограниченной среде. Теперь Git будет работать только с вашим кодом (около 50 файлов).
