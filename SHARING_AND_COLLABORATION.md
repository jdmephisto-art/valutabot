
# 🚀 Как отправить код в GitHub (Решение проблем)

Если у вас возникли ошибки `403 Forbidden`, `Permission denied`, `fatal: 'origin' does not appear...` или `Segmentation fault`, следуйте этой инструкции.

## Шаг 0: Исправление системной ошибки (Fork/Resource unavailable)
Если терминал пишет `Segmentation fault` или `bash: fork: retry: Resource temporarily unavailable`:
1. **Закройте текущую вкладку терминала.**
2. **Откройте новую вкладку терминала.** Это очистит память и позволит Git работать.

## Шаг 1: Настройка вашей личности в Git
Git должен знать, кто автор изменений. Выполните эти команды (подставьте свои данные):

```bash
git config --global user.email "your-email@example.com"
git config --global user.name "Your Name"
```

## Шаг 2: Создание токена доступа (PAT)
GitHub больше не принимает обычные пароли. Вам нужен **Personal Access Token**:
1. Перейдите в [GitHub Settings -> Tokens (Classic)](https://github.com/settings/tokens).
2. Нажмите **Generate new token (classic)**.
3. Выберите срок (например, 30 дней) и отметьте галочку **repo**.
4. Нажмите Generate внизу и **обязательно скопируйте токен**.

## Шаг 3: Гарантированная привязка и отправка (Push)
Выполните эти команды по очереди. Они удалят старые сломанные настройки и создадут новые:

```bash
# 1. Инициализация (если еще не сделано)
git init

# 2. ОЧИСТКА: Удаляем старую привязку, если она была (игнорируйте ошибки здесь)
git remote remove origin 2>/dev/null

# 3. ПРИВЯЗКА С ТОКЕНОМ: Замените <TOKEN> на ваш токен из Шага 2
# Формат: https://<TOKEN>@github.com/jdmephisto-art/valutabot.git
git remote add origin https://<ВАШ_ТОКЕН_ИЗ_ШАГА_2>@github.com/jdmephisto-art/valutabot.git

# 4. СОХРАНЕНИЕ: Если "nothing to commit", этот шаг можно пропустить
git add .
git commit -m "Final secure version"

# 5. ОТПРАВКА: Принудительно отправляем код в ветку main
git branch -M main
git push -u origin main -f
```

---
**Совет:** Если после выполнения этих команд GitHub все еще требует пароль — значит, токен в ссылке был введен неверно. Повторите Шаг 3 внимательно.
