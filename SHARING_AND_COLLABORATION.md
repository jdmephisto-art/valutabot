
# 🚀 Как опубликовать ВалютаБот

Вы скачали проект, и теперь его нужно выложить в интернет. Вот пошаговая инструкция специально для Windows (с использованием Git Bash, который у вас на скриншоте).

## Шаг 1: Подготовка на GitHub

1. Зайдите на [GitHub.com](https://github.com/) и войдите в аккаунт.
2. Нажмите **New repository**.
3. Назовите его `valuta-bot` и нажмите **Create**.
4. Скопируйте ссылку на репозиторий (например, `https://github.com/ваш-логин/valuta-bot.git`).

## Шаг 2: Загрузка кода (используем Git Bash)

1. Откройте папку с проектом на компьютере.
2. Нажмите **правую кнопку мыши** на пустом месте и выберите **"Open Git Bash here"**.
3. В открывшемся черном окне введите эти команды по очереди:

```bash
# Инициализация (если еще не сделано)
git init

# Добавляем адрес вашего GitHub
git remote add origin ВСТАВЬТЕ_ВАШУ_ССЫЛКУ_ЗДЕСЬ

# Если ссылка уже была, используйте:
# git remote set-url origin ВСТАВЬТЕ_ВАШУ_ССЫЛКУ_ЗДЕСЬ

# Подготовка файлов к отправке
git add .
git commit -m "Final production version"

# Отправка в облако
git push -u origin main
```

*Примечание: Если папка `.next` или `node_modules` попадают в список отправки (они весят много), не переживайте — файл `.gitignore` в корне проекта должен их отсечь.*

## Шаг 3: Публикация через Firebase App Hosting

1. Перейдите в [Firebase Console](https://console.firebase.google.com/).
2. Выберите ваш проект.
3. В меню слева: **Build** -> **App Hosting**.
4. Нажмите **Get Started**.
5. Подключите свой GitHub аккаунт и выберите репозиторий `valuta-bot`.
6. Нажмите **Next** и **Finish**.

Firebase сам соберет проект и даст вам ссылку (например, `valuta-bot.web.app`).

## Шаг 4: Подключение Telegram

1. Получите токен у `@BotFather`.
2. В настройках бота (Menu Button или Web App) укажите URL, который вам выдал Firebase.
3. Готово! Теперь ваш бот работает.

---
*Если при `git push` возникла ошибка авторизации, GitHub может потребовать "Personal Access Token" вместо обычного пароля. Это настраивается в Settings -> Developer Settings на GitHub.*
